interface:
  display_name: "Execution Estimation"
  short_description: "Estimate LoE, blast radius, and planning need"
  default_prompt: "Use $execution-estimation to estimate LoE, change footprint, blast radius, and whether planning is recommended before coding."
